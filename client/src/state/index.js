import { createSlice } from '@reduxjs/tooljit';

const initialState = {
    isCartOpen: false,
    cart: [],
    items: [],
}

export const cartSlice = createSlice({
    name: "cart",
    initialStaet,
    reducers: {
        
    }
})